# Developed under Python 3.9 (Anaconda).
# Research-critical libraries are pinned to the versions used for the paper,
# because they determine the ELM and metaheuristic behaviour.
intelelm==1.3.0
mealpy==3.0.3
permetrics==2.0.0
scikit-learn==1.6.1
imbalanced-learn==0.12.4
xgboost

# Data, plotting, and I/O (less version-sensitive)
numpy>=1.21
pandas>=1.4
matplotlib>=3.5
openpyxl
